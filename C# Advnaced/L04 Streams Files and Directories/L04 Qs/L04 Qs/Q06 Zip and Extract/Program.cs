using System;
using System.Collections.Generic;
using System.Linq;
using System.IO;
using System.IO.Compression;
public class Program
{
    public static void Main()
    {
        // ZipFile.CreatFromDirectory(@$"{path and fileName}, {path and fileName}.zip");
        // ZipFile.ExtractToDirectory(@$"{path and fileName}, {path}")
    }
}